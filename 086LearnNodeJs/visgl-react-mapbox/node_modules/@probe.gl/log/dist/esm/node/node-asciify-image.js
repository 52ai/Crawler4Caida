export function nodeAsciifyImage(_ref) {
  let {
    image,
    message = '',
    scale = 1
  } = _ref;
  let asciify = null;

  try {
    asciify = module.require('asciify-image');
  } catch (error) {}

  if (asciify) {
    return async () => {
      const data = await asciify(image, {
        fit: 'box',
        width: "".concat(Math.round(80 * scale), "%")
      });
      console.log(data);
    };
  }

  return async () => {};
}
//# sourceMappingURL=node-asciify-image.js.map