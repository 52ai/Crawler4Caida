"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.parsePbf = parsePbf;
exports.unpackProperties = unpackProperties;

var _pbf = _interopRequireDefault(require("pbf"));

function parsePbf(buffer, TileReader) {
  var pbf = new _pbf.default(buffer);
  var tile = TileReader.read(pbf);
  return tile;
}

function unpackProperties(properties) {
  if (!properties || !properties.length) {
    return [];
  }

  return properties.map(function (item) {
    var currentRecord = {};
    item.data.forEach(function (_ref) {
      var key = _ref.key,
          value = _ref.value;
      currentRecord[key] = value;
    });
    return currentRecord;
  });
}
//# sourceMappingURL=tile-loader-utils.js.map